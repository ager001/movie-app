import { View, Text } from 'react-native';
import React from 'react';

const saved = () => {
  return (
    <View className="flex-1">
      <Text>saved</Text>
    </View>
  );
};

export default saved;